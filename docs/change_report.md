# 修复前后对照（v4.5 一致性验收）

* 修复前基线：`docs/baseline_pre_fix.json`（2026-09-19T02:59:04.743984+00:00）
* 修复后基线：`docs/baseline_post_fix.json`（2026-09-19T03:05:48.154726+00:00）

原则：**保留旧版本供复现，但修复错误后允许结果变化；必须说明变化原因，不得为保持旧数字而保留缺陷。**

## 1. 源码摘要

| | 修复前 | 修复后 |
| --- | --- | --- |
| 源码摘要 | `d849e82df3cb4f56…` | `30c27d96e628b60b…` |
| 参与摘要的文件数 | 89 | 90 |

源码摘要已变化。

## 2. 测试与验收

| 项 | 修复前 | 修复后 |
| --- | --- | --- |
| 单元测试 | （无输出） | （无输出） |
| 端到端验收 | 全部验收检查通过 | 全部验收检查通过 |

## 3. 观测模式天梯（关键对照）

| 模式/策略 | 修复前 | 修复后 | 变化 | 预期 |
| --- | --- | --- | --- | --- |
| full/dqn | 0.4739 | 0.4739 | 0.0000（未变） | **必须一致**（full 不经测量层，是本轮的对照锚点） |
| full/fixed80w | -3.1954 | -3.1954 | 0.0000（未变） | **必须一致**（full 不经测量层，是本轮的对照锚点） |
| full/myopic | 0.4949 | 0.4949 | 0.0000（未变） | **必须一致**（full 不经测量层，是本轮的对照锚点） |
| full/random | -0.5866 | -0.5866 | 0.0000（未变） | **必须一致**（full 不经测量层，是本轮的对照锚点） |
| full/rule | 0.4636 | 0.4636 | 0.0000（未变） | **必须一致**（full 不经测量层，是本轮的对照锚点） |
| ideal/dqn | 0.3901 | 0.3723 | -0.0178 | 允许变化（见 §4 归因） |
| ideal/fixed80w | -3.1954 | -3.1954 | 0.0000（未变） | 允许变化但未变 |
| ideal/myopic | 0.4949 | 0.4949 | 0.0000（未变） | 允许变化但未变 |
| ideal/random | -0.5866 | -0.5866 | 0.0000（未变） | 允许变化但未变 |
| ideal/rule | 0.4636 | 0.4636 | 0.0000（未变） | 允许变化但未变 |
| realistic/dqn | 0.4385 | 0.0004 | -0.4381 | 允许变化（见 §4 归因） |
| realistic/fixed80w | -3.1954 | -3.1954 | 0.0000（未变） | 允许变化但未变 |
| realistic/myopic | 0.4227 | -0.9837 | -1.4064 | 允许变化（见 §4 归因） |
| realistic/random | -0.5866 | -0.5866 | 0.0000（未变） | 允许变化但未变 |
| realistic/rule | 0.4313 | -0.9837 | -1.4150 | 允许变化（见 §4 归因） |

## 4. 变化归因

| 编号 | 修复 | 影响面 |
| --- | --- | --- |
| F1 | **执行功率进入主动传感器**：`_sensor_context` 为受控雷达注入本步执行功率，`effective_tx_power_w()` 统一正演/反演 | realistic 的**检测结果与整条测量序列**（含随机数消耗顺序）；ideal 下 `force_detection=True`，检测不受功率影响，只有 `snr_db` / `rcs_est_m2` 派生量变化；**full 不经测量层，不受影响** |
| F2 | **观测空间逐字段上下界**：有符号维（bearing/elevation/range_rate）保留 `[-1, 1]` | ideal / realistic 的 12 维符号信息（由「被抹成 0」恢复为真实负值）；full 无有符号维 |
| F3 | **缺失率分母改为平台容量**（原为真值实体数） | ideal / realistic 的九维缺失率数值；full 不含该组特征 |

### 4.1 已确证的事实

1. **`full` 模式 5 个策略全部逐位未变** —— 本轮最重要的对照：三条修复都没有越界影响 legacy 路径，旧实验可原样复现。
2. **`ideal` 只有 DQN 变了**（+0.3901 → +0.3723），规则/随机/短视/固定功率四行未变。
3. **`realistic` 变化显著**：`rule` 与 `myopic` 同时落到**完全相同的 −0.9837**，`DQN` 落到 +0.0004。两个不同策略得到同一个数值，是**轨迹退化**的特征。

### 4.2 归因：已确定 / 已排除 / 未隔离

**已确定**：`evaluate_observation_modes.py::run_policy` 的注释写明，脚本策略在 `ideal`/`realistic` 下必须走**信念桥接**（只用观测重建状态），否则会直接读真值仿真器。所以这两行**是观测耦合的**——观测语义一变，动作就会变。这解释了为什么 `full` 不变而 `realistic` 大变。

**已排除**：F3 不是原因。`strategy/belief_policy.py` **不含** `meas_rate` / `MEASUREMENT_EXTRA_FEATURES` 的任何引用，也没有按索引读观测向量的代码，因此缺失率分母的改变不会经信念桥接影响策略动作。

**未隔离（明确留作待办，不猜结论）**：F1 与 F2 各自的贡献尚未分开测。已知边界：
* F1 会改变 realistic 模式的**检测帧**（功率低时探测不到），从而改变测量序列与信念重建，是 realistic 变化的主要嫌疑；
* F2 **恢复**了被抹掉的负值，本身只增加信息；但若信念桥接或指标收集代码隐含假设「这些维非负」，恢复负值会暴露该假设，从而产生行为变化。
* 隔离方法（下一阶段第一件事）：临时只回退 F1 / 只回退 F2，各自重跑 realistic 行并比较**动作序列**。本轮**没有做**，因此不写结论。

### 4.3 对结论的影响（必须一起读）

* **旧 checkpoint 不能用于修复后的策略对比**：`output/rl_ideal`、`output/rl_realistic` 是在**修复前观测语义**下训练的；修复后同一权重评测数字变化属**训练/测试输入语义不一致**，不是策略能力变化。可归因的策略对比必须**重新训练**。
* **`realistic/rule` 与 `realistic/myopic` 的塌陷是待查信号**，不得解释为「测量层/协同变差了」；隔离实验完成前，**不得**引用修复后的 realistic 行做任何能力判断。
* **修复前数字的复现**：把源码回退到摘要 `d849e82df3cb4f56…`（两侧完整输出都保留在本文件同级），重跑同一命令即可逐位复现。

